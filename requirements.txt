# Core dependencies
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
pyyaml>=6.0.0,<7.0.0

# Optional: LLM support (for local model inference)
# Install with: pip install -r requirements.txt -r requirements-llm.txt
transformers>=4.40.0,<5.0.0
accelerate>=0.25.0,<1.0.0
torch>=2.0.0,<3.0.0

# HuggingFace Hub for model downloads
huggingface-hub>=0.20.0,<1.0.0
